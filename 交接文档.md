# Death Trench 2D - 项目交接文档

## 📋 项目概览

| 项目 | 说明 |
|------|------|
| **名称** | Death Trench 2D |
| **类型** | 2D俯视角射击游戏 |
| **技术栈** | HTML + CSS + JavaScript (ES6) + Electron |
| **版本** | v1.4.0 |
| **状态** | 开发中（第一版） |

---

## 📁 文件结构

```
f:\ai\game\
├── index.html          # 主游戏页面
├── editor.html         # 存档编辑器页面
├── main.js             # Electron主进程入口
├── server.js           # HTTP开发服务器
├── api.js              # SVG生成API服务器
├── generate-images.js  # AI图片生成脚本
├── css/
│   └── style.css       # 全局样式
├── js/
│   ├── game.js         # 游戏核心逻辑（约900行）
│   ├── save.js         # 存档管理模块
│   ├── coord.js        # 坐标系统模块（未使用）
│   └── utils.js        # 工具函数模块（已创建）
└── picure/             # 图片资源目录
```

---

## 🔧 核心功能状态

| 功能 | 状态 | 说明 |
|------|------|------|
| 主菜单 | ✅ 正常 | 开始游戏、设置、教程、地图编辑器 |
| 游戏核心 | ✅ 正常 | WASD移动、鼠标瞄准射击 |
| 武器系统 | ✅ 正常 | 手枪/步枪/狙击枪，切换与换弹 |
| 敌人AI | ✅ 正常 | 追踪玩家、自动射击 |
| 道具掉落 | ✅ 正常 | 金币、医疗包、弹药、星星 |
| 黑市交易 | ⚠️ 部分 | 购买功能正常，出售/装备逻辑待完善 |
| 背包系统 | ⚠️ 部分 | inventory对象已添加，UI已实现 |
| 地图选择 | ✅ 正常 | 5种主题地图 |
| 存档系统 | ✅ 正常 | 本地存储+文件导入导出 |

---

## 🐛 已知问题

### 已修复
1. ✅ **速度值类型不一致**：已统一为百分比（默认100%）
2. ✅ **移速过快地图消失**：添加边界检查限制玩家移动范围
3. ✅ **对象池冲突**：移除ObjectPool，恢复简单数组系统
4. ✅ **语法错误**：修复多余括号导致的解析失败

### 待修复
1. ❌ **黑市交易逻辑不完整**：出售物品未检查库存，装备护甲逻辑需完善
2. ❌ **coord.js未集成**：坐标系统模块已创建但未在game.js中使用
3. ❌ **碰撞检测精度**：isBlocked使用Math.floor可能导致角色卡住
4. ❌ **性能优化**：子弹、敌人频繁创建销毁，建议后续实现对象池

---

## 📊 关键数据结构

### playerData（玩家数据）
```javascript
const playerData = {
    coins: 1000,           // 金币
    totalKills: 0,         // 总击杀数
    totalScore: 0,         // 总分数
    equippedArmor: '',     // 当前装备护甲（'light'/'heavy'）
    selectedMap: 'desert', // 选中地图
    inventory: {           // 背包
        medkits: 3,
        armor_light: 0,
        armor_heavy: 0,
        grenades: 2,
        ammoBox: 5,
        speedBoost: 1
    }
};
```

### player（游戏中玩家状态）
```javascript
player = {
    x: startX,       // 格子坐标
    y: startY,
    health: 100,
    maxHealth: 100,
    score: 0,
    kills: 0,
    angle: -Math.PI / 2,
    currentWeapon: 0,
    weapons: [...WEAPONS]
};
```

### settings（游戏设置）
```javascript
const settings = {
    difficulty: 'normal',  // easy/normal/hard
    playerSpeed: 100       // 百分比
};
```

---

## 🎮 游戏流程

```
主菜单 → 大厅 → 选择难度/地图 → 开始游戏 → 游戏中 → 游戏结束
              ↓              ↓                ↓
           设置面板       地图编辑器        黑市交易
```

### 游戏循环
```javascript
gameLoop() → update() + draw()
    ├── update(): 处理移动、子弹、敌人逻辑
    └── draw(): 绘制地图、玩家、敌人、子弹、UI
```

---

## 🚀 启动方式

### 开发模式
```bash
# 启动开发服务器
node server.js

# 访问 http://localhost:8080
```

### Electron模式
```bash
# 安装依赖
npm install

# 启动
npm start

# 打包（后续版本）
npm run build
```

---

## 🔑 核心函数

| 函数 | 文件 | 说明 |
|------|------|------|
| `startGame()` | game.js | 开始游戏入口 |
| `update()` | game.js | 游戏逻辑更新 |
| `draw()` | game.js | 渲染画面 |
| `shoot()` | game.js | 玩家射击 |
| `spawnEnemy()` | game.js | 生成敌人 |
| `collectDrop()` | game.js | 收集掉落物 |
| `gameOver()` | game.js | 游戏结束 |
| `SaveManager.save()` | save.js | 保存存档 |
| `SaveManager.load()` | save.js | 加载存档 |

---

## 📝 后续开发建议

1. **代码拆分**：game.js过于庞大（900行），建议拆分为多个模块
2. **对象池**：实现ObjectPool优化性能，避免频繁创建销毁对象
3. **测试**：添加单元测试和集成测试
4. **音效**：添加背景音乐和音效
5. **网络**：实现多人联机功能
6. **关卡系统**：设计多个关卡和Boss战
7. **成就系统**：添加成就和排行榜

---

## ⚠️ 注意事项

1. **不要使用ObjectPool**：之前引入的对象池系统导致冲突，已回退到简单数组
2. **速度值范围**：settings.playerSpeed使用百分比（50-150）
3. **边界检查**：玩家移动有限制，防止越界导致地图消失
4. **存档加密**：使用XOR加密，安全性较低，后续可改进

---

## 📞 联系方式

项目负责人：[用户]

---

*文档生成时间：2026-06