# 死亡战壕 - 剧情设计需求文档

> 供外部 AI 编写剧情内容使用。需与现有游戏系统（任务、邮件、NPC 对话、UI 弹窗）兼容。

---

## 1. 世界观

- **背景**：近未来，资源枯竭导致“黑潮军团”占领东部资源带。
- **阵营**：
  - Death Trench 特遣队（玩家所属，松散佣兵/抵抗组织）
  - 黑潮军团（敌方，重甲、生化实验、纳米控制技术）
- **基调**：硬核军事、道德灰色、生存与代价。
- **场景**：沙漠前哨、断壁城（城市废墟）、毒藤密林、荒漠补给线、森林撤离点。

---

## 2. 核心角色

| 角色 | 身份 | 语言/行为特征 | 剧情作用 |
|------|------|---------------|----------|
| 普莱斯 | 指挥官 | 军衔感、任务第一、偶有隐瞒 | 发布主线任务，隐藏与黑潮的过去 |
| 幽灵 | 战术支援 | 冷静、数据导向、不信任任何人 | 提供情报，引导“真相”分支 |
| 新兵（玩家） | 特遣队新兵 | 由玩家选择决定口吻 | 推动分线剧情 |

---

## 3. 分线剧情（三主线）

玩家在关键对话中选择分支，影响后续任务与邮件。

### 3.1 忠诚（loyalty）
- **关键词**：服从、任务优先、牺牲平民
- **触发**：选择“服从命令”“炸掉军火库”
- **专属任务**：截击黑潮补给线（wasteland，击杀 35）
- **角色关系**：信任普莱斯，与幽灵疏远

### 3.2 真相（truth）
- **关键词**：调查、证据、普莱斯的秘密
- **触发**：选择“想知道黑潮在做什么”“自己查”
- **专属任务**：实验室渗透（ruins，击杀 30）
- **角色关系**：跟随幽灵线索，怀疑普莱斯

### 3.3 仁慈（mercy）
- **关键词**：少死人、保护平民、人道撤离
- **触发**：选择“少死人”“先确认平民撤离”
- **专属任务**：撤离难民（forest，保护撤离点）
- **角色关系**：与平民/医疗线相关

---

## 4. 章节大纲

### 第一章：入伍
- 场景：大厅首次进入
- 对话：`intro_price`（普莱斯）
- 选择分支 → 设置 `storyState.branch`

### 第二章：断壁城
- 场景：完成 `task_kill2` 后
- 对话：`city_choice`
- 选择：炸军火库 / 救平民 → 强化或切换分支

### 第三章：真相浮现
- 场景：完成 `task_kill3` 后（truth 分支）
- 对话：`ghost_warning`
- 邮件：`ghost_warning_mail`

### 后续章节（待扩展）
- 根据分支解锁专属任务与结局导向。

---

## 5. NPC 对话规范

### 数据结构
```javascript
DIALOGUES['dialogue_id'] = {
    speaker: '名称 · 身份',
    avatar: 'emoji',
    tag: '所属阵营',
    lines: [
        { text: '单行文本' },
        { text: '带选项文本', choices: [
            { text: '选项 A', action: () => { setStoryBranch('loyalty'); advanceChapter(); } },
            { text: '选项 B', action: () => { setStoryBranch('truth'); } }
        ]}
    ]
};
```

### 编写要求
- 每段对话不超过 3 行。
- 选项不超过 3 个。
- `action` 只改 `storyState`：分支、flag、chapter。
- 不要直接修改 `playerData` 或任务列表。

---

## 6. 任务简报规范

### 数据结构
```javascript
MISSION_GUIDES['task_id'] = {
    title: '任务标题：副标题',
    story: '剧情背景，可换行',
    objectives: [
        '① 地图与模式要求',
        '② 击杀/目标数量',
        '③ 战术提示'
    ],
    preDialogue: '可选的前置对话 ID'
};
```

### 编写要求
- `title`：地点 + 行动代号。
- `story`：2-4 行，包含角色台词或环境描写。
- `objectives`：3 条，用 ①②③ 编号。
- 与当前任务 `target`、`map`、`reward` 保持一致。

---

## 7. 剧情邮件规范

邮件用于在任务间推进剧情，存储于 `deathTrench_mails`。

```javascript
{
    id: 'mail_id',
    sender: '发件人',
    subject: '主题',
    content: '正文（支持换行）',
    date: 'YYYY-MM-DD',
    read: false,
    type: 'story'
}
```

- 剧情邮件 `type` 固定为 `'story'`。
- 重要剧情转折必须通过邮件二次确认。

---

## 8. 技术约束

- 剧情状态键：`deathTrench_story_state`
- 状态字段：`{ chapter, branch, flags, completedDialogues, seenBriefings }`
- 所有对话 ID 必须唯一，避免与现有 ID 冲突。
- 新增分支任务 ID 需以 `task_` 开头，避免重复。
- 公告内容只包含玩家相关信息，不暴露技术/加密细节。
- 剧情内容需适配 8080（普通版）和 3030（开发版）两个入口。

---

## 9. 现有剧情 ID 清单

- 对话：`intro_price`, `intro_ghost`, `city_choice`, `ghost_warning`, `ch4_truth_confront`, `ch4_loyalty_order`, `ch4_mercy_civilian`, `ch5_final_choice`, `ending_loyalty`, `ending_mercy`, `ending_truth`
- 简报：`task_kill1`~`task_kill3`, `task_boss1`, `task_truth_lab`, `task_loyalty_convoy`, `task_mercy_rescue`
- 邮件：`ghost_warning_mail`, `price_after_city`, `ch4_truth_meeting`, `ch4_loyalty_directive`, `ch4_mercy_eileen`, `ch5_ending_mail`
- 结局标志：`ending_sacrifice`（忠诚线）/ `ending_rescue`（仁慈线）/ `ending_truth`（真相线），完成后设置 `game_completed`
